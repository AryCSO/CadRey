import 'package:flutter/material.dart';

class DialogCliente extends StatelessWidget {
  const DialogCliente({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}